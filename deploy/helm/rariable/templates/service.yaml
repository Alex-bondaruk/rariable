apiVersion: v1
kind: Service
metadata:
  name: {{ include "rariable.fullname" . }}
  labels:
    {{- include "rariable.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "rariable.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
